# API token for Bearer auth on /v1/* endpoints.
# Leave empty to disable auth (useful for local dev).
API_TOKEN=

# OpenAI API key. Without it, the gpt-4o-transcribe model is not registered.
OPENAI_API_KEY=

# Path to the YAML config inside the container.
CONFIG_PATH=/app/config/config.yaml
